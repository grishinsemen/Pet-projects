# 🚀 Инструкция по загрузке на GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите **"New repository"** (зелёная кнопка)
3. Заполните:
   - **Repository name:** `hh-job-market-analyzer`
   - **Description:** `Анализатор рынка IT-вакансий HeadHunter с подбором карьеры для INTP`
   - **Public** (чтобы показывать на интервью)
   - ✅ **НЕ** ставьте галочки на README, .gitignore, license (у нас уже есть)
4. Нажмите **"Create repository"**

---

## Шаг 2: Загрузите код

Откройте терминал в папке проекта и выполните:

```bash
# Добавить все файлы
git add .

# Создать коммит
git commit -m "Initial commit: HH Job Market Analyzer"

# Подключить удалённый репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/hh-job-market-analyzer.git

# Отправить код на GitHub
git push -u origin main
```

**Если git попросит логин/пароль:**
- Используйте Personal Access Token вместо пароля
- Создать токен: GitHub → Settings → Developer settings → Personal access tokens → Generate new token

---

## Шаг 3: Улучшите README (опционально)

### Замените placeholder баннер на реальный

В `README.md` замените строку:
```markdown
![HH Job Market Analyzer Banner](https://via.placeholder.com/1200x400/1a1f3a/ffffff?text=HH+Job+Market+Analyzer)
```

На одно из:

**Вариант 1: Без баннера (просто удалите строку)**

**Вариант 2: Создайте баннер на [Canva](https://canva.com)**
- Размер: 1200x400px
- Загрузите в репозиторий как `assets/banner.png`
- Используйте: `![Banner](assets/banner.png)`

**Вариант 3: Используйте shields.io бейджи**
```markdown
[![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)](https://www.python.org/)
[![HH API](https://img.shields.io/badge/API-HeadHunter-red.svg)](https://dev.hh.ru/)
```

---

## Шаг 4: Добавьте Topics на GitHub

На странице репозитория нажмите ⚙️ **Settings** → добавьте topics:
- `python`
- `job-search`
- `career-analytics`
- `headhunter-api`
- `data-analysis`
- `mbti`
- `intp`

---

## Шаг 5: Что показывать на интервью

### Сильные стороны проекта:

✅ **Практическое применение:**
- "Я создал этот инструмент для анализа рынка вакансий при поиске работы"
- "Помог мне понять, какие навыки наиболее востребованы"

✅ **Технические навыки:**
- Работа с REST API
- Парсинг и очистка данных (HTML, regex)
- Агрегация и статистический анализ
- Работа с JSON

✅ **Аналитическое мышление:**
- "Разработал систему оценки совместимости вакансий с личностью (INTP Score)"
- "Автоматизировал извлечение требований из 200+ вакансий"

✅ **Самостоятельность:**
- Pet-проект без туториалов
- Решение реальной задачи

### Как презентовать:

```
"Это мой pet-проект для анализа рынка IT-вакансий. 

Я использовал API HeadHunter для парсинга вакансий системных 
аналитиков и риск-направлений. Скрипт автоматически извлекает 
требования, агрегирует статистику по навыкам и зарплатам.

Самая интересная часть - INTP Career Analyzer. Он оценивает 
10 IT-ролей по критериям: уровень кодинга, доступность для 
джунов, возможность удалёнки. Помог мне выбрать направление 
для развития карьеры.

Технологии: Python, requests, regex для парсинга, Counter 
для агрегации. Результаты экспортируются в JSON."
```

---

## Бонус: Добавьте GitHub Actions (опционально)

Создайте `.github/workflows/test.yml`:

```yaml
name: Test

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run syntax check
        run: python -m py_compile *.py
```

Это добавит зелёный бейдж ✅ "passing" на вашем README.

---

## Готово! 🎉

Ваш проект теперь на GitHub и готов для показа на интервью!

**Ссылка будет:**  
`https://github.com/YOUR_USERNAME/hh-job-market-analyzer`
